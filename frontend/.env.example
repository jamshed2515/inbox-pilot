# Frontend Environment Configuration
# Local development uses /api (forwarded by Vite dev proxy to http://localhost:5000)
# In production (e.g. Vercel), set this to your deployed backend URL, e.g.:
# VITE_API_BASE_URL=https://inbox-pilot.onrender.com/api
VITE_API_BASE_URL=/api
